name: Build and Deploy Static Site

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Node.js
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci --prefer-offline

  #     - name: Run ESLint
  #       run: npm run lint

  build-site:
    # needs: [lint]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up bun, vite, and typescript environment
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 'latest'
          cache: true

      - name: Install dependencies
        run: bun install

      - name: Build the app
        run: bun build
      - name: Generate sitemap
        run: bun run generate-sitemap
      - name: Generate robots.txt
        run: bun run generate-robots-txt
      - name: Generate Open Graph images
        run: bun run generate-og-images

  deploy:
    needs: [build-site]
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4